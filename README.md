# LR
大致思路是：

1.sigmoid函数，输入w和xi的内积，是一个常数，输出一个[0,1]的常数；

2.因为此时数据符合伯努利分布，根据概率质量函数P(y)，得到似然函数Loss(w)，这个函数是个凸函数，要求最大值，导数为0最大，求所有w的偏导数，即为求w梯度，

3.梯度下降，每求一次梯度，w更新一次，

求出w后，验证一下精度，

0.9361702127659575

还行，
